# Archived Work Log

- Archived at: 2026-08-26T03:46:47.987578+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-24 — 链路烟测与渲染器缺陷修复

- 用进程内模拟 Provider（`httpx.MockTransport`）跑通「冻结 Config → 渲染 prompt-message-contract.v1 → OpenAI-compatible Gateway JSON/SSE → 严格 Schema → accepted 结构化结果」链路；模型偷换与 Schema 拒绝均 fail closed。烟测脚本在 `/tmp/ms_image_gateway_smoke.py`，未入库。
- 修复 `PromptRenderer` 在替换安全变量后误判 `}}` 为残留占位符的缺陷：嵌入的规范化 JSON（如嵌套输出 Schema）合法地以 `}}` 结尾，现在只拒绝未解析占位符或未闭合的 `{{`；补回归测试，测试从 48 项到 49 项。
- 本机 MySQL 在 3306 已启动，但当前迁移需先在既有 schema 上 baseline/stamp（非空库 bootstrap），故本轮未做真实 DB 迁移烟测。
