# Archived Work Log

- Archived at: 2026-08-30T07:13:20.390789+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-28 — 严格 JSON fence 与 definite-response receipt 修复

- 在 Gateway 输出边界增加严格单一完整小写 `json` Markdown fence 解包；裸 JSON 保持兼容，解包后继续执行原冻结 Draft 2020-12 Schema。前后说明、无/错误标签、多 fence、非法 JSON 继续 fail-closed。
- 新增 definite response 异常合同：HTTP 拒绝、HTTP body JSON 解析失败、Provider message/模型/usage/冻结 Schema 解析失败均携带预先构建的脱敏 image receipt、manifest SHA 和发送数量；pre-send 合同失败与 unknown delivery 不伪造 receipt。
- `GatewayClient` 将 HTTP 成功后 body JSON/对象/request-id 归一化失败标为确定响应解析失败；配置缺失仍是 pre-send RuntimeError，网络/超时仍是 unknown。
- Worker 单独消费 definite response failure，并把发送事实交给 `AIRequestService.finalize_attempt_failure()`；该 Service 在同一事务写 Attempt，并投影到无 Winner Call。partial audit 或 unknown + definite receipt 被拒绝。
- 扩展既有 `test_ai_gateway_attempt_contracts.py`，没有新建测试脚本；静态检查、compileall、定向 64 tests 与 backend 全量 110 tests 均通过。
- 重启 harness 管理的当前源码 Worker，复用真实 VD+Lateral Study 创建 Task `baef4d8619494a75aac7c2c7ef805c87`；最终 completed、合法 `abnormal`、Report final。DAL 对账证明 Snapshot v3 与 receipt v2 的两图顺序/SHA/projection/provenance、manifest 和数量一致，receipt 无 signed URL。
- Worker 重启时管理器额外补拉 Relay，短暂形成两个 current-source Relay；精确停止旧 Relay `19803` 后恢复一个 Relay `36534` 与一组 Worker `36544/36551`。
- 未新增医学规则、projection 规则、结果字段修补、表、字段或迁移；真实输出不作为医学准确率证据。
