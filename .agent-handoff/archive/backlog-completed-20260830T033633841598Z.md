# Archived Backlog Completed

- Archived at: 2026-08-30T03:36:33.841610+00:00
- Reason: backlog exceeded 32 KiB; archived completed checklist items

## Original Content

- [x] 将 `AI_PLATFORM_OPENAI_BASE_URL` 与 `AI_PLATFORM_API_KEY` 成对安全注入真实 imaging Worker 启动环境（不恢复 `AI_GATEWAY_*`）；2026-08-27 已用同一冻结 Task 验证 OSS signed GET、MySQL、RabbitMQ/Celery、Provider、Attempt/Stage/Report happy-path，且 Snapshot 无敏感键。
- [x] 跑通 Outbox（事务发件箱）→ Worker（工作进程）→ OSS（对象存储）→ Provider（模型提供方）→ Attempt/Stage finalize（物理尝试/阶段终态化）→ DecisionFinalization（结果定稿）→ Report（报告）的 Primary happy-path；completed duplicate、两类 cancel/late-result、unknown lookup/lease 与有界终止均已通过。JWT/Artifact signing 经用户确认不计入当前核心链。
- [x] 真实验证同一 completed JointPrimaryReader Outbox 的 RabbitMQ duplicate delivery：Worker 幂等确认，Call/Attempt/Report 与状态/摘要均不变。
- [x] 验证 cancel-before-provider 与其 duplicate delivery：Task/Stage 明确取消，Call/Attempt/Report 为 0，未进入网络边界。
- [x] 验证 provider-sent cancel/late-result 与 unknown Attempt 的安全 unsupported/lease 恢复证据。
- [x] 接入并真实资格化 `imaging.reconcile_ai_attempts` 自动周期调度；独立 Celery Beat singleton 为默认关闭的唯一仓库 owner，显式 route、重叠 CAS、SIGKILL/lease 恢复与无替代 POST 均已验证。
- [x] 完成 P1-B unknown 持久有界终止：正式迁移、count/age CAS、可信终态优先、默认 unsupported 技术失败链和无 Report/替代 POST 均已资格化。
