# Archived Work Log

- Archived at: 2026-08-28T09:15:47.458588+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-27 — unknown Attempt lookup/reconcile 真实资格化

- 真实 MySQL due unknown 候选首次运行暴露 `AIAttemptReconcileWorker` 把 ORM candidate 带出事务；正式 session 为 `expire_on_commit=True`，因此 claim 前触发 `DetachedInstanceError`。两次失败隔离探针都在 `finally` 中删除 qualification Call/Attempt，Attempt 总数恢复为 3。
- 修改现有 `apps/backend/workers/imaging_worker/ai_attempt_reconcile.py`，在查询事务内冻结 `attempt_id/state_version` 标量；扩展现有合同测试验证事务退出后不再访问 ORM candidate。
- 定向测试 `10 passed`，backend 全量 `94 passed, 22 warnings`；Ruff、compileall、`git diff --check` 通过。
- 真实 Connection `xray_primary_platform@1.0.0` 声明 `supports_request_lookup=false`、`supports_idempotency_key=true`；正式 Worker 对隔离 unknown Attempt 安全返回 `unsupported`，保持同一 provider idempotency identity/Provider Request ID，不新建 Attempt、不改变 Call。
- 正式 Service 提交 30 秒 claim 后模拟崩溃：未到期不重复 claim，到期后 Worker 成功恢复并再次安全 reschedule。既有 Task/Stage/Call/Attempt/Report 摘要不变，qualification 数据已清理。
- 审计确认 `imaging.reconcile_ai_attempts` 只有 task 注册，当前 compose 没有 beat/cron/周期发送；自动调度未资格化。unsupported unknown 也缺少持久有界终止字段；未经授权未改表、未生成迁移。
