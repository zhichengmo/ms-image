# Archived Work Log

- Archived at: 2026-08-28T08:39:40.360888+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-27 — XRay Primary 正式 Worker happy-path 真实资格化

- 修复现有 DalBase CAS 回读与 Task/Call/Attempt/Stage ORM 局部刷新问题，补齐 `AICallDal.get_by_id_for_update()`；Worker 跨事务仅保留 Stage 标量 ID。
- 真实 Platform 合同探针确认 `strategy=single` 返回 400，`strategy=race` 返回 200；将 ms-image Gateway payload 对齐 `ms-ai-fast`。该字段仅是 Platform 调度合同，不启用 ms-image 多 Provider Race。
- 通过现有 `ImagingExecutionService.reconcile_expired_stages()` 收敛旧 lease；旧 400 Task 为 failed，旧 deadline Task 达恢复上限后为 dead_letter。
- 用隔离 topology 启动 Relay/Worker；Worker 只从 `ms-ai-fast/.env` 注入 `AI_PLATFORM_OPENAI_BASE_URL + AI_PLATFORM_API_KEY`，未输出配置值。
- 创建第三个真实冻结 diagnose Task `7c37b9b659bb4b8e98b6a6076923c985`，同一 Task 真实完成 StudyPreparation、JointPrimaryReader、DecisionFinalization 和 Report。
- 非敏感证据确认 Study/Series/Image ready；三个 Outbox published；AICall/AICallAttempt succeeded；Provider Request ID/response SHA/parsed result 存在；原始响应对象引用不存在；Report final；Task Snapshot 无敏感键。
- 资格化 Worker/Relay 已停止。duplicate/cancel/unknown/late-result 真实环境仍未运行，医学准确率仍未知。
