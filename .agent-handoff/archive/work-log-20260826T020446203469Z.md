# Archived Work Log

- Archived at: 2026-08-26T02:04:46.203724+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-24 — AI Gateway 与 Prompt Runtime D1/D2/D4 实现

- 延续 Gateway 合同：新增 `apps/backend/core/ai/gateway/image_signer.py`（Attempt 级短期签名 URL 边界，默认 disabled），并修正 `openai_compatible.py` 未用导入。
- 新增 Alembic `20260824_02`：`ai_prompt_template` 增加 `message_contract_json/source_receipt_json/source_receipt_sha256`，`ai_config_record` 增加 `prompt_message_contract_json/prompt_source_receipt_sha256/gateway_profile_json`，`ai_call_record` 增加 `rendered_messages_json/attempt_count/winner_attempt_id`，新建 `ai_call_attempt_record`（无 FK/enum/tenant_id、状态 string、downgrade guard）。
- `AIRequestService` 增加 Attempt 三段生命周期（`prepare_structured_call` / `execute_gateway_attempt_network` / `finalize_attempt` / `finalize_attempt_failure` / `prepare_retry_attempt` / `load_attempt_for_network`）与运行时门禁 `_runtime_gate_allows()`；v2 disabled 路径冻结 `rendered_messages_json`，不改变 disabled 语义。
- `core/config.py` 增加 `AI_GATEWAY_*` 与 `AI_PROMPT_NACOS_*` 设置（默认 namespace `c0cc9e0e-0fed-4faf-bc57-e9bab46a78a9`）；requirements 显式声明 `jsonschema==4.23.0`、`qj-nacos==0.2.0`。
- 新增 Nacos Prompt 导入：`prompt_source.py` + `prompt_import_service.py` + `POST /ai-prompts/import`；拒绝 Jinja/过滤器/函数/属性访问/$变量/未映射占位符，variant→default 仅 404 兜底。
- 测试从 24 项扩到 45 项：新增 Gateway JSON/SSE/错误归类/模型偷换/Schema 拒绝/签名 URL、多消息组装、Profile 规范化、导入校验与回执、运行时门禁、Attempt/Call 模型约束；异步用例改用 `@pytest.mark.anyio`（本机未装 pytest-asyncio）。
