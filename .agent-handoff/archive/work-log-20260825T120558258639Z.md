# Archived Work Log

- Archived at: 2026-08-25T12:05:58.258860+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-24 — AI Prompt 控制面迁移隔离 MySQL 演练

- 在 Docker daemon 不可用的条件下，使用本机 MySQL 9.3.0 创建一次性隔离数据库和仅限该库的临时用户；没有触碰已有 `ms_image` 或其他业务库。
- 用当前 A-C schema 作为已在 head 的起点完成 `stamp head → downgrade base → upgrade head`，并核验控制面四表、Config/Call v2 列与索引、Audit 幂等唯一约束、无 FK/ENUM/Attempt 表和 Alembic marker。
- 实际演练暴露两项问题并修复：`str(URL.create(...))` 会以 `***` 隐藏密码导致 Alembic 在线连接认证失败；MySQL 9.3.0 拒绝 `DATETIME DEFAULT utc_timestamp(6)`。`env.py` 改用 `render_as_string(hide_password=False)`，模型/migration 使用 `CURRENT_TIMESTAMP(6)`，应用和 Alembic 连接以 `init_command` 设置 `time_zone='+00:00'`。
- 通过临时 v2 Config 行验证 downgrade guard 必然以 `ai_prompt_control_plane_downgrade_blocked_by_v2_config_rows` 失败，guard 失败后 schema 与 marker 保持 head；随后删除 probe 行。
- 以真实应用 async engine 插入/删除一次 Prompt probe，验证 session 时区为 `+00:00` 且新表时间戳默认值与 UTC 相符；最后删除临时数据库、临时 MySQL 用户和 `/tmp` 凭据文件。

## 2026-08-24 — ms-ai-fast 接入分支建立

- 用户要求基于当前 HEAD `492a249c98175bf818ce092451585db766d73e76` 建立独立接入分支；已创建并切换至 `codex/ms-ai-fast-integration`。
- 分支创建前后均未修改业务源码、配置、迁移或测试；切换后立即核验既有 73 项未提交工作树条目仍被保留。
- 真实 Provider、Secret 解析、网络发送和 Physical Attempt 仍未开始；后续实现需先明确与 ms-ai-fast 的适配范围和验收合同。
