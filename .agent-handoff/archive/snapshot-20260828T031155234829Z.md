# Archived Snapshot

- Archived at: 2026-08-28T03:11:55.234987+00:00
- Reason: snapshot exceeded the soft limit (19978 bytes, 166 lines)

## Original Content

# 代理交接当前快照

## 当前目标与状态

- 最后更新：2026-08-28（Session `complete/cancel` 与非终态 Task 生命周期联动已实现并完成合同验证）
- 工作区：`/Users/mozhicheng/workspace/code/cy-code/ms-image`
- 当前分支：`codex/prompt-runtime-ai-gateway`
- 当前接口审计与补齐权威：`docs/refactor/30-xray-interface-by-interface-audit-and-development-checklist.md`；医学/运行后续顺序仍以 29 号为准；28 号保留 C1 前裁决，27 号仅保留候选设计背景。
- `POST /series` 已获用户授权并完成局部合同修复：Study 行锁与 Series current read 串行化并发创建；新 Series 与 Study manifest/revision 在同一请求级事务内推进，幂等重放不重复推进，CAS 失败整体回滚；未改表、Schema、迁移或测试文件。Report publish/void 与 Evaluation/Alembic 阻断仍未处理。
- `GET /tasks?id=` 已补 `current_report_id`、`started_at`、`finished_at`、`next_retry_at`；直接复用已有 ORM/物理列，不改 Service/DAL/表/迁移。Task `retry_wait/next_retry_at` 当前没有生产写入路径，字段会为空，不能误报为已实现 Task 级重试状态机。
- `GET /images/page` 已按 `API -> ImageService -> ImageDal(DalBase)` 补齐：必传 `series_id`，支持 status/role/current/history/page 过滤；默认选择每个 logical key 的绝对最新工作流版本，`include_versions=true` 优先于 `current_only`。owner 只沿 Series -> Study -> Session 校验一次；分页 DTO 和 ORM 列加载均排除对象存储、KMS、validation lease 和大 JSON 内部字段；未改表、迁移或测试文件。
- `GET /tasks/page` 已按 `API -> TaskService -> TaskDal(DalBase)` 补齐：至少传 `session_id` 或 `study_id`，支持 execution status、task type、UTC 时间闭区间和 1..100 分页；Service 校验 Session/Study owner 与 scope 一致性，TaskDal 再强制 requester 过滤并在 Session 查询时关联 Study。返回精简 `TaskStatusResponse`，不含 Snapshot/预算/Pipeline hash/错误消息/取消原因；未改表、迁移或测试文件。
- `GET /reports/current?task_id=` 已按 `API -> ReportService -> ReportDal(DalBase)` 补齐：Task 不存在或 requester 不匹配统一 404；合法空 pointer 返回 `data=null`；pointer 指向不存在、其他 Task 或 `void/superseded` Report 时 fail-closed 为 404；只返回 `final/published`。未改表、字段、迁移或测试文件。
- Session 生命周期已采用“完整诊断会话”语义：`complete` 锁 Session 并拒绝仍有非终态 Task 的会话；`cancel` 同事务锁定全部非终态 Task、按 first-request-wins 写取消请求，再 CAS 取消 Session；`POST /tasks` 共用 Session 行锁并拒绝在终态 Session 下创建全新 Task。未改 API、表、字段、迁移或测试文件。
- 当前状态：`INTERFACE_INVENTORY=COMPLETE / INTERFACE_IMPLEMENTATION=INCOMPLETE / L1_RECOVERY=MINIMUM_CLOSED / C1_HAPPY_PATH_IMPLEMENTED / C1_BOUNDARY_HARDENING_PENDING / MEDICALLY_VALIDATED=UNKNOWN / MEDICAL_RELEASE=NO-GO`。
- 保留运行事实：`LOCAL_CODE_FULL_CHAIN=E2E_PASSED`、`PRIMARY_WORKER_HAPPY_PATH=PASSED`、duplicate/cancel/late-result/unknown unsupported safety/lease recovery 均已有真实证据。
- 全链路分层判定：L0 单病例 Primary happy-path 已通过；L1 上游稳定恢复最小闭环已完成，但仍需发布环境并发/性能资格化；L2 生产运行/运维和 L3 Evaluation/医学发布仍未完成。接口盘点完成不等于接口开发完成或生产/医学资格化完成。

## 本轮完成

- `docs/refactor/30-xray-interface-by-interface-audit-and-development-checklist.md` 已逐项覆盖上述 75 个真实 method/path，并于 2026-08-28 新增 L0/L1/L2/L3 全链路接口闭环总判定，拆清真正新增接口、已有接口修正和非 HTTP 内部阻断。
- 首个接口缺陷已修复：ready Study 新增 Series 后立即推进 revision、重算 manifest、状态回 validating 并清空 ready_at；Session/Task 生命周期联动也已完成；Runtime readiness、derived Image、Prompt import 和 Control Plane readiness 等缺口仍待逐项处理。
- `/series` 只读存量扫描未发现 ready Study 下 `Series.created_at > Study.revision_changed_at` 的矛盾行；真实数据库回滚事务验证 revision `+1`、幂等重放 `+0`、payload 冲突拒绝和 CAS 失败整体回滚均通过。并发资格化证明不同 key 均成功、相同 key 返回同一资源，所有临时数据已清理。
- Task 轮询合同已验证：OpenAPI 包含四个 nullable 字段；当前 9/9 终态 Task 有 `finished_at`、4 个有 current Report、执行前取消允许 `started_at=null`；正确 owner 可读、错误 owner 仍被拒绝；backend `94 passed, 22 warnings`。
- Image 分页合同已验证：OpenAPI 参数和精简 DTO 正确；真实 MySQL 回滚事务验证 v1 ready/v2 uploading 时默认只返回 v2、历史分页可恢复两版且 status 过滤不回退旧版；正确 owner 成功、错误 owner 被拒绝；最终 backend `94 passed, 22 warnings`。
- Task 分页合同已验证：OpenAPI 暴露 8 个查询参数和精简 DTO；SQL 同时包含 requester、Session/Study、status/type/time 与稳定排序；Service 的 owner/mismatch 分支通过；真实 MySQL 只读验证 Study、Session+status/type、精确时间闭区间、分页和错误 owner 隔离；最终 backend `94 passed, 22 warnings`。
- Report current 合同已验证：OpenAPI 注册 `/api/v1/reports/current` 且响应允许 null；8 类内存分支均通过；真实 E2E Task 的 current final Report 可按 owner 读取，错误 owner 被拒绝；最终 backend `94 passed, 22 warnings`。
- Session/Task 生命周期合同已验证：active Task 阻止 complete，terminal Task 不阻止 complete；cancel 只为非终态 Task 写取消请求且不强制终态，保留首次原因/时间，重放不重复推进版本；终态 Session 拒绝新 Task。Ruff、compileall、backend `94 passed` 和真实 MySQL 回滚事务均通过。
- 只读核验 Runtime 主要业务接口均保持 `API -> Service -> DalBase CRUD -> Model/DB`，ID 均位于 query/body，Session/Study/Image/Task/Report 的资源归属、幂等/CAS 与 finalize 前置校验主体合理。
- 发现 Report 状态接口为真实阻断：`ReportService`/`ReportDal` 使用 `report.state_version` 和 `cas_put_data()`，但 Report ORM、物理表和 `ReportResponse` 均无该字段；publish/void 路由存在不等于可调用。void 还缺 reason/actor/audit，publish 不改变 Runtime 可见性，交付语义未定义。
- 发现 Task 合同缺口：27 号声称的 stage 摘要实际不在 `TaskResponse`，且不应直接加入普通 Runtime。Study detail 只返回 Study+Series，不返回 Image；Image page 与 Task page 已补齐，Session/Study 的产品列表接口仍按真实消费者后置。
- 只读核验 MySQL：主库 `ms_image` 有 20 张 ORM 表 + `alembic_version`，ORM/物理列、普通索引、唯一约束一致；所有业务表均为独立 `id` 单列主键，无 foreign key、无数据库 Enum、无缺失 column comment；核心父子关系孤儿计数均为 0。
- 确认 Evaluation 边界不可运行：四张 Evaluation 表错误存在主库且均为空，配置目标库 `ms_image_eval` 不存在（1049）；所有 Evaluation Model 与在线 Model 共用 `BaseModel.metadata`，Alembic 仅连接 `MYSQL_DB` 且没有表过滤。
- 确认迁移链不能从零重建当前库：两个 revision 只创建 Prompt/Connection/Pool/Audit/Attempt 等少数表，并假定 `ai_config_record` 等表已存在；当前 20 表来自 create_all + stamp，不是完整 migration replay 证据。
- 当前数据：10/10 Image projection 为空；Task 为 9 行（含 3 行非法 `produced`）、Report 为 4 行（含 3 行非法 `produced`）；current Report pointer 未发现漂移；未修改任何业务数据。
- 核验 DeepSeek 的 C1 完成报告：`ImagingExecutionService._project_medical_status()` 已从嵌套 `complete_medical_result.medical_status` 投影，`ReportService.finalize()` 已拒绝 `produced` 等非法列入参；v1 Stage 仍保留 `produced/not_produced`。
- 独立复跑 `PYTHONPATH=. python -m pytest -q apps/backend/tests`：`94 passed, 22 warnings`。
- 确认真实 C1 E2E Task `f125fb933401403b89b7e1cd954c4f31` 的 Task/Report/content medical status 均为 `review_required`，Stage output 仍为 `produced`；存量非法 `produced` 行未修。
- 发现 C1 未完全加固：当前 helper 会把多种损坏组合静默降为 `not_produced`；允许嵌套 `not_produced`；不检查 availability/payload 一致；状态集合重复；Report 不验证列值与 content 一致。
- 确认现有 94 个测试没有直接覆盖 `_project_medical_status()`、非法状态组合或 Report content/列冲突；Provider 坏 JSON 在更早边界失败，不能替代 C1 negative contract。
- 用户既有决定保持：不建设人工复核工作流；不做 Report webhook/通知，调用方轮询 Task 和 Report。
- 本轮修改 Session/Task DAL 与 Service、Task Schema/Endpoint 及现有 30 号审计文档；未修改数据库表/字段、迁移或测试文件，生命周期真实数据库验证在显式回滚事务内完成且无数据残留。

## 关键新裁决

- 当前不需要全链重写；问题集中在少数状态/数据边界、读合同和数据库部署边界，继续采用保留入口的局部修复与内部模块化重构。
- “路由/Service/Model 已存在”不能标为接口完成：Report publish/void 和整个 Evaluation Plane 均属于代码存在但当前运行不可用。
- 29 号文档在进入代码实施前必须补入 Report CAS/交付语义与在线/Evaluation 迁移拓扑；DeepSeek 的“本期零表零迁移”只覆盖其候选新增接口，不覆盖既有 broken Admin Report 合同和数据库可重建性。
- 产品接口分四类：必须修现有合同（Report CAS/delivery、C1.1；Session/Task 生命周期已完成）；必须贯通既有入口（projection、clinical context）；P2 新增（Study/Session page、Report view、可选 stage detail/segmentation/intake）；明确不新增（webhook、人工复核、sessions/context、fixed-bank、projection summary）。Task/Image page 与 Report current 已按逐项授权完成。
- Session `cancelled` 的稳定语义是取消请求已原子接受，不表示响应时所有 Task 已经收敛；调用方必须继续轮询 Task 至 `completed/failed/cancelled/dead_letter`。
- DeepSeek 的 C1 happy-path 结论成立，但“C1 完全闭环”不成立；先做 C1.1，再做 P1-A。
- C1.1 对 `produced + 合法四值 complete result` 原样投影；`not_produced + 无 complete result` 兼容；其他组合均 fail-closed，不创建 final Report，不从 Findings 猜状态。
- P1-A 不能只在 `celery_app.py` 加 Beat schedule；必须明确唯一 scheduler owner、queue route、本地/生产进程定义、聚合观测、重叠 CAS 和非人工自动触发真实验收。
- P1-A 不解决 unsupported 无限循环；P1-B 仍需持久 `first_unknown_at/reconcile_count` 或等价事实和迁移授权。
- 医学证据链顺序为 D1 projection -> E1-MV -> C2 result v2 -> D2 context -> M1；等待 P1-B/P1-C 外部决定时可以推进 D1，但不能跳过 P1 release Gate。
- segmentation 保留为 P2 产品需求；派生 Image role 只是基础，仍需 source manifest、metadata schema、object identity、查询、权限、版本/删除和生命周期合同。

## 下一动作

1. 下一在线输入合同建议处理 D1：在 Image prepare/replace 贯通逐图 projection/provenance -> canonical manifest -> Task Snapshot -> Prompt。
2. 随后处理 D2：`POST /tasks` 以严格 allowlist 冻结 clinical context，不新增 Session context。
3. 拆分 Runtime、AI Control、Evaluation Control readiness；Admin 只保留可降级聚合视图。
4. 由用户确认 Report 治理语义，并在任何新迁移前先修在线/Evaluation metadata 与 Alembic 可重建基线；C1.1/P1/E1-MV/C2/M1 仍按 29 号 Gate 推进。

## 当前阻断与风险

- Admin Report publish/void 当前会在真实 DAL CAS 路径访问不存在的 `Report.state_version`；请求又无法从 `ReportResponse` 获取 expected version。
- Report publish/void 的交付语义未闭合：final 已可被 Runtime 读取，publish 不改变可见性；void 后 Task 仍 completed、pointer 清空，但历史与按 ID 查询仍返回作废内容，且无 reason/actor/audit。
- Alembic 与 metadata 没有在线/Evaluation 分离且无法从空库重建；在此基础上直接增加 Report/P1-B 字段会继续扩大不可重放迁移债务。
- 自动 reconcile 周期触发不存在；当前只有 Celery task 注册。
- unsupported lookup 默认可永久重排；Attempt 只有 `next_reconcile_at`，没有稳定 first-unknown/count 事实。
- Runtime/Admin JWT 与 Artifact signing 生命周期未真实资格化。
- `ms_image_eval` 目标库不存在；Evaluation 表误建在主库，获批 Gold、医学 Scorer、denominator 和 Artifact signing 也尚未完成。
- 存量旧 Task/Report 仍可能持久化 `produced`；未经授权不得回填，也不得从 Findings 推断。
- projection 在 API/write/manifest/snapshot/prompt 仍断链；clinical context 仍未进入 Task Snapshot。
- CompleteMedicalResult v1 的 Finding/Coverage/SourceRef 结构宽松，Report view 和细粒度医学评分不能稳定消费。
- `review_required` 只是 AI 医学终态，不代表人工复核完成或医学批准。
- 现有 backend 测试集没有 `StudyService.create_series` 自动回归用例；按用户“未特别要求不生成测试脚本”的约束，本切片使用真实数据库回滚事务验证，未新增测试文件。
- `Task.next_retry_at` 已进入响应，但当前 Task 主链没有写入 `execution_status=retry_wait` 或该时间；Stage/Outbox/Attempt 的重试状态不能冒充 Task 级轮询状态。
- Image 分页 EXPLAIN 在当前稀疏真实库只证明使用 logical-version 索引且无全表扫描；稳定排序仍有 `Using filesort`。当前每个 Series 最大仅 1 条，尚未在大 Series 数据量下资格化，不应据此新增迁移；上线前需用生产等量级数据复测。
- Task page 现有 requester/study/session-created 索引足以支撑首版合同，真实库只验证了小样本正确性；大 Session、多 Study、状态/时间组合的执行计划和延迟尚未等量资格化，未据此新增索引或迁移。
- Session/Task 生命周期的真实 MySQL 验证使用单连接显式回滚事务；跨连接 create-vs-complete、create-vs-cancel、worker-vs-cancel 时序尚未在发布环境资格化。
- 终态 Session 下已有 business key 的 Task 重放仍执行原有 active Config、Snapshot/hash 和 Study revision 幂等校验；不能笼统承诺所有历史重放都直接返回原 Task。

## 活动文件

- `docs/refactor/29-xray-post-c1-development-guide.md`（当前实施权威）
- `docs/refactor/30-xray-interface-by-interface-audit-and-development-checklist.md`（当前接口逐项审计与开发清单）
- `docs/refactor/README.md`
- `docs/refactor/28-xray-evidence-driven-development-guide.md`（C1 前裁决）
- `apps/backend/services/runtime/service/imaging_execution_service.py`
- `apps/backend/services/runtime/service/report_service.py`
- `apps/backend/services/runtime/api/api_v1/endpoints/reports.py`
- `apps/backend/tests/test_ai_gateway_attempt_contracts.py`
- `apps/backend/workers/imaging_worker/celery_app.py`
- `apps/backend/workers/imaging_worker/ai_attempt_reconcile.py`
- `apps/backend/services/runtime/service/ai_attempt_reconcile_service.py`
- `apps/backend/core/imaging/manifest.py`
- `apps/backend/services/runtime/service/task_service.py`
- `apps/backend/services/runtime/service/session_service.py`
- `apps/backend/crud/session.py`
- `apps/backend/schemas/task.py`
- `apps/backend/crud/task.py`
- `apps/backend/services/runtime/api/api_v1/endpoints/tasks.py`
- `apps/backend/schemas/image.py`
- `apps/backend/crud/image.py`
- `apps/backend/services/runtime/service/image_service.py`
- `apps/backend/services/runtime/api/api_v1/endpoints/images.py`
- `apps/backend/services/runtime/service/study_service.py`
- `apps/backend/services/runtime/stages/xray/prompt_commands.py`
- `.agent-handoff/snapshot.md`
- `.agent-handoff/decisions.md`
- `.agent-handoff/backlog.md`
- `.agent-handoff/risks.md`
- `.agent-handoff/work-log.md`
- `.agent-handoff/validation.md`

## 未运行或未知

- Report publish/void 真实数据库调用：`NOT RUN`（静态合同已确认必然缺少 CAS column；未执行写操作）。
- Alembic 空库 replay：`NOT RUN / EXPECTED TO FAIL`（迁移源码只创建 5 张表并假定其他表已存在）。
- Report final/publish/void 的最终产品交付语义：`USER DECISION REQUIRED`。
- C1.1 focused negative contracts：`NOT IMPLEMENTED / NOT RUN`
- 自动周期调度真实运行：`NOT RUN`
- unsupported unknown 持久有界终止：`NOT IMPLEMENTED`（需字段/迁移授权）
- Runtime/Admin JWT、Artifact signing lifecycle：`NOT RUN`
- D1/E1-MV/C2/D2：`NOT IMPLEMENTED / NOT RUN`
- 医学 Gold/M1/Paired A/B/Holdout：`UNKNOWN / NOT RUN`
- `GET /images/page` 大 Series 排序延迟：`UNKNOWN / NOT RUN`（当前样本每个 Series 最大 1 条，EXPLAIN 有 filesort）。
- `GET /tasks/page` 大 Session/多 Study 查询延迟：`UNKNOWN / NOT RUN`（当前真实库仅完成小样本正确性验证）。
- Session/Task 跨连接并发资格化：`NOT RUN`（当前已完成行锁顺序审计和真实 MySQL 单事务合同验证）。

## Current State

- Last updated: 2026-08-28
- Last agent: Codex
- Workspace root: `/Users/mozhicheng/workspace/code/cy-code/ms-image`
- Current objective: 逐接口补齐 XRay 全链路；本轮已完成 Session complete/cancel 与非终态 Task 生命周期联动。
- Current status: L0 Primary happy-path 已通过；L1 最小恢复闭环已完成但跨连接并发/等量性能未资格化；L2 生产运行与 L3 Evaluation/医学发布仍未完成。
- Immediate next actions:
  - D1：贯通 Image projection/provenance -> canonical manifest -> Task Snapshot -> Prompt。
  - D2：为 POST /tasks 增加严格 allowlist clinical context，并冻结进 Snapshot。
  - 拆分 Runtime、AI Control、Evaluation Control readiness，Admin 保留 degraded 聚合。
  - 决定 Report final/publish/void 治理语义，并在迁移前修复 Alembic/Evaluation metadata 基线。
- Active files:
  - `docs/refactor/30-xray-interface-by-interface-audit-and-development-checklist.md`
  - `docs/refactor/29-xray-post-c1-development-guide.md`
  - `apps/backend/services/runtime/service/session_service.py`
  - `apps/backend/services/runtime/service/task_service.py`
  - `apps/backend/crud/session.py`
  - `apps/backend/crud/task.py`
  - `.agent-handoff/risks.md`
  - `.agent-handoff/backlog.md`
- Blockers: Report CAS/delivery、C1.1、自动 reconcile/unknown 有界终止、readiness 跨平面耦合、Alembic/Evaluation 分库、JWT/Artifact signing 和医学 Gold/M1 尚未关闭。
- Open questions:
  - Report 是否停用 publish，并为 void/revision CAS 授权 state_version/reason/actor/audit？
  - Session/Study page 与宽 Report view 是否有明确消费者需要？
  - 跨连接 Session/Task 竞态何时在发布环境资格化？

## Recovery Summary

- Session `cancelled` 只表示取消请求已原子接受，调用方继续轮询 Task 至终态。
- Task 终态为 completed/failed/cancelled/dead_letter；已请求取消但仍执行中的 Task 仍属非终态。
- 终态 Session 下已有 business key 的 Task 仍执行原 Study/Config/Snapshot/hash 幂等校验。
- 本轮未改 API、Model、Schema、表、字段、迁移或测试文件；backend 全量 94 passed。
- 继续开发前先读 30 号接口清单和目标源码，不整体照搬 27 号候选设计。
