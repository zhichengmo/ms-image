# Archived Work Log

- Archived at: 2026-08-30T06:10:48.900137+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-28 — D1 逐图 projection 四层链

- prepare direct/multipart Schema 新增必填 projection；只做 trim、非空和现有 `VARCHAR(64)` 长度校验，不转换大小写、不新增医学枚举或字符白名单，UNKNOWN 由调用方显式传。replace direct/multipart 省略继承、显式值更正、显式 null 拒绝。
- Service 在既有 `technical_metadata_json` 写入保留 `projection_provenance`；新写只允许 `caller_declared/xray-projection.v1`，调用方提供保留 key 会在 Schema 边界拒绝。
- Image Runtime response/page response 显式返回 projection provenance；历史空值只投影为 `UNKNOWN/legacy_unspecified`。
- canonical manifest 增加 `series-image-manifest.v2` 和完整冻结校验；旧 manifest 算法保留为 `build_series_manifest_legacy()`，只用于旧 Task replay。
- `TaskService` 通过 `ImageDal.list_ready_for_series_ids()` 一次批量读取 ready Image，校验 Series/Study hash 后冻结 `task-request-snapshot.v3` ordered images；request SHA 自然覆盖 projection/provenance/order。
- Prompt v3 严格校验完整 Study snapshot，再投影逐图 image id/version/hash/order/projection/provenance；view_positions 来自同一 refs，不从文件名/图像猜测。
- AI Request v3 只从 Snapshot 构建 Provider object inputs；旧 v2 仍实时查询并使用 legacy hash。新增无 signed URL 的 `ai-image-receipt.v2`，写入 Attempt 与 Call。
- 更新既有本地 E2E 脚本，让无法确认投照位的 smoke 图片传 `UNKNOWN`；同步 29/30 号既有开发文档。未新建文档、测试文件、表、字段或迁移。
- 真实数据库只读发现 8 张 ready Image 全为 projection NULL、8 个 Series 全为 legacy manifest；9 个已冻结 v2 Task 全部仍可重放。本轮没有回填或改写存量事实。
- 用户明确要求不再增加 projection 规则后，撤销 uppercase 与字符 token 限制；当前只保留 trim、非空和现有 `VARCHAR(64)` 长度边界，调用方大小写和 Unicode 文本原样保存。
- 复核发现 Image page 的精简列加载遗漏 provenance 所在 `technical_metadata_json`，已补入既有 `load_only()`，避免异步序列化时 deferred-load；未增加接口或业务校验。
- 最终 Ruff、compileall、`git diff --check`、大小写/Unicode 保留、OpenAPI 无 enum/pattern、真实 MySQL Image page 只读序列化和 backend 全量 `94 passed, 22 warnings` 均通过。
- 复核 DeepSeek 的 D1 P0 报告：确认 5 个 ready legacy Study 创建新 Config v2 Task 会被强制 v3 校验阻断；纯内存验证 legacy Study 报 `task_series_manifest_snapshot_mismatch`，手工重算后的 D1 Study 可构造 Snapshot v3。
- 否定其 `object_version_id` 顺序根因和“从 SHA 删除该字段”的修复建议：当前代码先完成 Image CAS/readback 后 recompute，真实 ready Image 的 object_version_id 全为 NULL；`e3f976…` 当前 Series/Study manifest 一致。
- 发现本地同一 validation queue 有 2026-08-27 与 2026-08-28 两组 Celery Worker 并存，新上传落 legacy manifest 很可能由旧 Worker 消费。本轮未停止进程、未修改业务代码或数据库。
- 实施 Task Snapshot legacy/D1 兼容选择：按每个 Series 的 stored SHA 同时核对 D1/legacy builder；全 legacy 保留 Snapshot v2，全 D1 冻结 v3，mixed/neither 复用既有 mismatch fail-closed。Study aggregate 继续使用唯一 `build_study_manifest()`，不新增第二算法。
- 真实库验证 5 个 ready legacy Study 选择 v2、1 个新 D1 ready Study 选择 v3；`e3f976…` 可构造 v3。真实 mixed/neither 组合均拒绝。
- 在回滚事务内分别创建 legacy queued/v2 与 D1 queued/v3 Task，并验证 legacy AI Request 可解析全部图片输入；回滚后无 Task 残留。未改表、迁移、Schema、测试文件或现有开发文档。
- Ruff、compileall、diff check 和 backend 全量 `94 passed, 22 warnings` 通过。DeepSeek 已清理旧 Worker并验证新链 completed/review_required/Report final；当前 API 仍需重启加载本轮兼容代码。
