# Archived Work Log

- Archived at: 2026-08-30T06:42:45.068124+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-28 — 使用真实数据运行 D1 多投照位 E1-MV

- 盘点 `/Users/mozhicheng/workspace/documents/X光_没问题_全量过滤`：43,931 JPG 与 43,931 paired JSON；确认 JSON `metadata.Position` 为投照位来源，未把文件名 `Ab/Ch/Mu`、宽高或模型输出当 projection。
- 固定同一检查元数据的犬腹部 `VD + Lateral` 两张不同 SHA 图片，重启当前源码 API/Relay/Worker，创建新的 Session/Study/Series/Image/Task 真实事实；未修改或回填存量数据。
- 验证 Image ready、Series manifest v2 stored/rebuilt SHA、Task Snapshot v3、Prompt 逐图 refs 的 ID/SHA/顺序/projection/provenance 全部一致。
- 两个正式 Task 均到 Platform HTTP 200，但因 `gemini-3.5-flash` 返回 fenced JSON 而 fail-closed；同一幂等键诊断重放取得完整响应形态，去 fence 后原文通过冻结 Schema。
- 新发现 parse failure 路径不落 `image_count_sent/sent manifest/ai-image-receipt.v2`，Report 也未生成；因此 D1 只能标记真实运行到 Provider，不能标记 E1-MV 全链完成。
- 新发现 `run_local_chain.sh` 在未定义 `PYTHONPATH` 的 shell 下因 `set -u` 启动失败；本轮通过显式 `PYTHONPATH=''` 启动，未修改启动器源码。
- 父级 DeepSeek harness 自动补拉被停止的 Relay/Worker，导致测试期间短暂存在两组 current-source 消费者；不是旧/新算法污染。收尾已停止本轮 launcher 的额外 Relay/Worker，保留 harness 管理的一组当前源码消费者与本轮最新 API。
- 本轮没有修改业务源码、数据库表/字段、迁移或测试文件；仅更新 durable handoff。
