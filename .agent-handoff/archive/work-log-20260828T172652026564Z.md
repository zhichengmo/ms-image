# Archived Work Log

- Archived at: 2026-08-28T17:26:52.026868+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-27 — 首个逐接口切片：修复 `POST /series`

- 用户授权先修 `/series`；完整读取 endpoint、Schema、StudyService、Study/Series DAL/Model、canonical manifest、事务依赖和错误映射。
- 修改 `StudyService.create_series()`：仅新插入 Series 时重算 Study manifest，并以既有 `state_version + revision_id` CAS 推进 revision、状态回 validating、清空 ready_at；幂等重放直接返回，CAS 失败由请求事务整体回滚。
- 新增 Study/Series DAL locking read，并让 create_series 先锁 Study：不同 key 并发串行各推进一次；相同 key 并发读取当前赢家并返回同一资源。
- 提取 `_advance_study_revision()`，让 Series 创建和 Image 变化复用同一套 Study manifest/completeness/revision 规则。
- 更新 30 号逐接口清单，将 §5.4 和 Slice 1 标为已完成；未改 API 路径、请求/响应 Schema、数据库表、迁移或测试文件。
- 上线前只读扫描未发现 ready Study 下晚于当前 revision 的存量 Series；真实数据库验证均在事务回滚后复核无测试 Series 残留。
- 独立真实 MySQL 并发资格化创建临时 Session/ready Study：两个不同 key 产生两个 Series，两个相同 key 只产生一个 Series，总 revision/state_version 均准确 `+3`；临时三条 Series、Study、Session 已删除并复核不存在。
