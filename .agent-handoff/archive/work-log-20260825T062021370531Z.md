# Archived Work Log

- Archived at: 2026-08-25T06:20:21.370725+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-21 — Backend Monorepo 交付、镜像与 Worker 运行合同

- 已核对暂存边界后提交并推送 `3c005d8 refactor: align backend monorepo service layout`；提交只包含 `apps/backend` 重组与 Docker/Compose/Alembic/CI 联动，没有纳入用户已有 docs/handoff 改动。
- Docker Desktop 已启动，`apps/backend/Dockerfile` 构建成功为 `ms-image-applications:monorepo`。在 `--network none` 的容器中导入三个 ASGI 应用和两个 Worker，三个 Uvicorn lifespan 均短时成功启动，两个 Celery Worker CLI 均可解析；没有启动 Compose 或连接真实依赖。
- 固化 Worker delivery 合同并推送 `a0d903b chore: freeze worker delivery runtime contract`：每个队列默认单并发、每进程单预取、保持 late ack/worker-lost reject；两个 Worker 的 Compose `stop_grace_period` 增至默认 hard task limit 120 秒外加 10 秒。扩展应先增加副本；若调整 lease/hard limit，也需同步调整 Compose 停机窗口。
