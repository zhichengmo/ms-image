# Archived Backlog Completed

- Archived at: 2026-08-30T04:12:57.156336+00:00
- Reason: backlog exceeded 32 KiB; archived completed checklist items

## Original Content

- [x] 在不改 v1/global 的前提下，FamilyRouting v2 已以确定性、零模型调用的方式验证 Primary 提供的唯一 Family/Focus 候选。
- [x] v2 受控词汇当前支持胸腔、腹腔、四肢骨关节、轴骨骼四个 Family；`head_neck` 无获批 Focus 时明确不路由。
- [x] 已接入最多一次 TargetedReview（专项复核），读取同一完整 Study（检查），输出新的完整病例结果。
- [x] 写入 Nacos 非医疗 smoke Prompt，并完成 `NacosPromptSourceClient → PromptRenderer → PromptMessageAssembler → GatewayClient → ms-ai-platform → gemini-3.5-flash` 严格 Schema 网络烟测；旧 SecretResolver/GatewayAdapter 实现随后已删除。
- [x] 数据库 Model schema、Prompt/Connection/ModelPool/Config、冻结 Control Plane、OSS signer 与 Worker Platform 主链均已完成；从空库 Alembic replay 仍是独立待办。


- [x] 创建 Task 的 request body 已增加 `species`；`diagnose` 必须传 `cat` 或 `dog`，并在 Task 创建期归一化、校验和冻结进 `request_snapshot_json`。
- [x] XRay Prompt Source 已收敛为唯一内部 key `xray_primary` 和 exact-only `common` variant；禁止 `cat/dog/default -> common` fallback。
- [x] Primary/Targeted Prompt command 均从冻结 Snapshot 把 `species` 写入 `SAFE_STUDY_CONTEXT_JSON`；Worker 不回查宠物档案、不猜测物种。
- [x] 已发布并回读校验 `ms-image.x-ray.primary.common.zh-CN@1.0.0`；本地正文位于 `prompts/xray/nacos/primary/common/zh-CN/`，历史 cat/dog/default 发布物只保留为不可变历史，不导入、不激活。
- [x] 使用既有控制面导入 common Prompt，建立 Connection/ModelPool，编译并激活唯一 `xray_diagnose` `global/global` Config。
