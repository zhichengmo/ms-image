# Archived Work Log

- Archived at: 2026-08-28T06:58:13.489906+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-27 — 发布 XRay canonical Primary Prompt

- 只读盘点当前运行时 Prompt 身份，确认 v2 XRay Nacos 只接受 `xray_primary/common`，目标 dataId 为 `ms-image.x-ray.primary.common.zh-CN`；猫犬由冻结 `SAFE_STUDY_CONTEXT_JSON.species` 条件化，不新增 cat/dog Prompt。
- 新建本地目录 `prompts/xray/nacos/primary/common/zh-CN/`，保存 `ms-image.x-ray.primary.common.zh-CN.v1.0.0.txt`。Prompt 仅声明 `SAFE_STUDY_CONTEXT_JSON` 与 `OUTPUT_SCHEMA_JSON`。
- 本地完成 cat/dog × primary/targeted 四种渲染检查、`$schema` 字面量保护、旧变量/旧状态残留扫描和 SHA-256 计算。
- 使用目标 namespace 发布 `1.0.0`，随后按 prompt key + version 精确回读；正文逐字一致、SHA-256 一致、变量集合一致、输出格式为 JSON、Schema 一致。未写入 Secret。
- 未修改路由、数据库、模型、迁移、Service、Worker 或运行时业务代码；未执行控制面导入/激活、真实 Task 或医学验证。
