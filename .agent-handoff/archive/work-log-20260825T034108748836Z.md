# Archived Work Log

- Archived at: 2026-08-25T03:41:08.748974+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-21 — Runtime 正式迁移到 apps/runtime

- 将活动 Runtime 路径统一为 `apps/runtime/`：Dockerfile COPY、Alembic import root、CI 边界、README/USAGE/CLAUDE、08/20 号当前交接文档和启动注释均已修正。
- 保留 `apps/runtime/app/` 作为当前兼容 Python 包，避免一次性改写全部 `from app...` 导入；`apps/ai_control/` 与 `apps/evaluation_control/` 只记录为已确认边界，未创建空壳服务。
- 活动源码和文档扫描不再发现 `services/runtime` 或 `services.runtime` 引用；历史 `docs/history/` 与 QJ 外部参考中的原始路径保持不变。
- 未修改用户已有 `apps/runtime/app/models/evaluation.py` 格式草稿；未生成迁移/测试脚本，未连接真实数据库、Broker、OSS 或 Provider。
