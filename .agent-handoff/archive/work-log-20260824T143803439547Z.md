# Archived Work Log

- Archived at: 2026-08-24T14:38:03.439796+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-20 — MR-1F Runtime Foundation 只读缺口复核

- 读取用户粘贴材料、交接状态、Monorepo Prompt、重构决策框架及相关 Runtime 源码；确认 MR-1 已完成唯一 `services/runtime` 源迁移，API/Worker/Relay/Reconcile 业务链已有实现。
- CONFIRMED：`evaluation/replay/__init__.py:3-9` 仍导入已删除的 `workers.xray_accuracy_worker.replay`，仓内无活跃消费者；外部消费者仍 UNKNOWN，不能恢复旧链或未经授权删除兼容入口。
- CONFIRMED：启动脚本仅在自身进程注入 `services/runtime`；仓库根的 `app`/`workers`/旧入口均不存在，Docker 通过 `/app` 包装复制运行；root-only Python 模块合同尚未冻结。
- CONFIRMED：`OutboxRelay.run_forever()` 与 Evaluation Relay 无限循环没有 stop event/signal bridge；Compose 的 Worker/Relay 没有独立 healthcheck 或 stop_grace_period；`run_servers.py` 仍用 `process.terminate()`。
- CONFIRMED：`app/core/async_db.py:66-101` import 时创建未被当前 Runtime 使用的 MS-HD engine/session；在线与 Evaluation engine 仍是当前业务依赖，不应一并延迟或改语义。
- INFERRED：下一写入切片应只处理 Foundation 外壳和验证合同；AI Control/Evaluation Control 拆分、Stage/Task/Report/Prompt/医学逻辑和真实 Provider 均不属于 MR-1F。
- 本轮未修改运行源码、Compose、迁移、Prompt 或受保护的 `services/runtime/app/models/evaluation.py`，未读取 Secret、连接真实基础设施或执行 Runtime import/Docker build。
