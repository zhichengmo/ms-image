# Archived Work Log

- Archived at: 2026-08-30T13:21:22.005892+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-30 — 猫狗 2–5 图 Runtime 真实矩阵资格化

- 通过独立 AI Control 的 `/health` 与 `/readiness` 门禁；临时 HS256 Admin Secret/Token 仅保存在进程内，未写文件、未打印。
- 实时读取猫狗 global Primary 3.0.0，并用 compile preview 证明新版本完整复用 Prompt、ModelPool、Connection、Output Schema、Pipeline 与 capability；唯一变化为 Config `3.0.1` 和 `max_input_images=5`。
- 分别执行 create、validate、带旧 active ID/state version 的 CAS activate；新 3.0.1 active，旧 3.0.0 retired，未覆盖或删除历史 Config。
- 使用现有 launcher 收敛到 API=1、Relay=1、Worker parent/child=1、Beat=0、broker consumer=1；database、Redis、RabbitMQ 和 worker readiness 全绿。
- 串行执行 cat-02/03/04/05 与 dog-02/03/04/05；8/8 均 Task completed、Report final、C2 v2、receipt v2，receipt image count 与 manifest N 一致，Config/Prompt SHA 前后无漂移。
- 将 8 份脱敏 evidence 写入 `docs/evidence/xray-2to5-runtime/20260830T130608Z/`；扫描未发现 Token、Signed URL、绝对影像路径、Prompt/Provider/Report 正文。
- 回归通过：`234 passed, 41 warnings`，Ruff、compileall、E2E help/非法参数、launcher shell、四种 Compose 配置均 PASS。
- 观察到每次 Harness 退出后的 aiomysql event-loop 析构告警；因退出码与所有业务/evidence 门禁均 PASS，本切片只记录风险，不临时修改代码。
- 正常停止本轮 launcher 与 AI Control；确认 8002/8010 释放、ms-image API/Relay/Worker/Beat=0、lock absent、imaging queue consumer/messages/unacked=0。
