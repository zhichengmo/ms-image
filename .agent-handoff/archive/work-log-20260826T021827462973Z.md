# Archived Work Log

- Archived at: 2026-08-26T02:18:27.463120+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-24 — 按代码审查修复 AI Gateway/Prompt Runtime 问题

- `prompt_import_service._fetch_nacos` 增加 `namespace_id` 参数，并按 `payload.namespace_id` 覆盖默认 namespace，修复导入回执 namespace 与实际拉取不一致。
- `prompt_source.NacosPromptSourceClient.fetch` 对 200 但非 JSON 对象的响应抛 `prompt_source_nacos_payload_invalid`，避免被当作 404 触发 variant fallback。
- `AIRequestService.finalize_attempt`/`finalize_attempt_failure` 增加已终态 Attempt 的幂等返回（复用 `_terminal_attempt_response_or_conflict`），修复重放时的 CAS 冲突。
- `config_compiler._validate_prompt` 与 `PromptTemplateService` 校验 message contract 的 `user_context_keys` 必须在 `variables_json` 声明，把错误提前到控制面。
- 测试从 45 项扩到 48 项，覆盖 message contract 变量缺失（两层）、Nacos 畸形 200 fail closed。
