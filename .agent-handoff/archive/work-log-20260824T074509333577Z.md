# Archived Work Log

- Archived at: 2026-08-24T07:45:09.333733+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-20 — 旧 AI 链路最终数据库收敛设计

- 只读审计旧 `ai_config`、`gpt_config_item`、`ai_prompt_template`、`ai_model_pool`、`ai_api_connection` 和 pool override 的模型、解析服务及当前 Runtime 冻结链。
- 最终设计保留 `ai_prompt_template`、`ai_api_connection`、`ai_model_pool`、`ai_config_record`、`ai_call_record`、`ai_call_attempt_record` 六类核心表，另以 `ai_control_audit_record` 保存只追加的后台治理事实；删除/不恢复 `gpt_config_item`、旧 `ai_config` 平行表和 `ai_config_pool_override`。
- Prompt 采用单表多版本完整正文；Config 激活时冻结 Prompt、Provider Plan、Schema、预算和 Pipeline 快照；Runtime 不直接读取可编辑 Prompt/Pool。
- 未修改业务代码、ORM、数据库文档或迁移；未生成测试脚本，未连接真实基础设施。
