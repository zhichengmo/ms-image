# Archived Work Log

- Archived at: 2026-08-28T09:20:44.510094+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-27 — 本地无 Docker XRay 全链路跑通

- 用户决策：不需要 Docker，直接用代码把 XRay 全链路跑起来。
- 本地依赖确认：MySQL 3306（ms_image 21 表，active `xray_diagnose` Config 已激活，旧 outbox 全部 `published` 不会重发）、RabbitMQ 5672、Redis 6379、ms-ai-platform 8060、OSS `ms-sz` 均可达。
- 启动三个本地进程：Runtime API（127.0.0.1:8010，dev RSA JWT + 公钥注入）、Outbox Relay（`python -m apps.backend.workers.imaging_worker.outbox_relay`）、Celery Worker（`celery -A ...celery_app worker --queues=imaging.image.validate`，AI_PLATFORM_* 从 ms-ai-fast/.env 注入进程环境，不落盘）。
- 一次性修正：e2e 脚本 `image_kind` 必须用合法枚举 `instance`（`source` 被拒绝）；`GET /studies` 响应为嵌套 `StudyDetailResponse`（`study` 对象内含 `revision_id`/`state_version`）。
- 全链真实通过（dog thorax jpeg 1612x1352）：上传 -> 校验 -> finalize -> Task -> Outbox -> RabbitMQ -> Worker -> 平台(provider_request_id=chatcmpl-*, actual_model=gemini-3.5-flash) -> Attempt -> Call -> 3 Stage -> Report final -> Task completed/produced；task_id `8ca384148d8440fa88ee01bfda81c9dc`。
- 新增仓库文件：`scripts/dev/run_local_chain.sh`、`scripts/dev/run_e2e_local.py`、`scripts/dev/gen_dev_keys.py`、`scripts/dev/issue_dev_token.py`；`scripts/dev/keys/` 加入 `.gitignore`（dev-only，不得提交）。
