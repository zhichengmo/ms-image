# Archived Work Log

- Archived at: 2026-08-28T16:22:26.132568+00:00
- Reason: work log exceeded 64 KiB or 30 dated sections

## Original Content

## 2026-08-27 — C1 实施复核与 29 号 post-C1 开发指南

- 独立核验 DeepSeek C1 报告：代码和真实 E2E 的 happy-path 结论成立；v1 Stage 保持 `produced`，新 Task/Report/content 已持久化嵌套 `review_required`。
- 发现 C1 尚未加固的异常合同：缺失/未知/冲突组合会静默变成 `not_produced`，嵌套 `not_produced` 被接受，状态集合重复，Report content 与列值不校验。
- 搜索现有测试确认没有直接覆盖 `_project_medical_status()`、`report_medical_status_invalid` 或 content/列冲突；Provider 坏 JSON 证据不直接覆盖 C1 持久化投影边界。
- 独立复跑 backend 全量：`94 passed, 22 warnings`。
- 新增 `docs/refactor/29-xray-post-c1-development-guide.md`（1050 行）：定义 C1.1、P1-A/B/C、D1/E1-MV/C2/D2/M1、单变量 A/B 和 P2 轮询/报告/segmentation/intake 的合同、write set、Gate、Stop/Rollback 与决策项。
- 更新 `docs/refactor/README.md`、`AGENT_HANDOFF.md` 与最小 handoff 状态，使 29 号成为当前 post-C1 实施权威；C1 happy-path 标记完成，下一最小切片改为 C1.1。
- 本轮只修改开发文档和 durable handoff；未修改业务代码、数据库、表、字段、迁移或测试脚本。

## 2026-08-27 — 四应用 API、ORM 与真实 MySQL 只读审计

- 完整读取 27/29 号文档，并对 Runtime/Admin/AI Control/Evaluation Control 做动态路由与 OpenAPI 盘点；无重复 method/path，无业务路由遗漏 response model。
- 对 Session/Study/Series/Image/Task/Report、AI Control、Evaluation API 的 Schema -> Endpoint -> Service -> DAL -> Model 链做抽查；核心分层和 caller/resource/control scope 主体合理。
- 新发现 Report Admin 状态合同断裂：Report 缺 `state_version` ORM/物理列/Response，ReportDal 却调用默认版本 CAS；publish/void 不能视为真实可用。并确认 void 缺 reason/actor/audit，publish/void 与 Runtime 可见性/Task delivery state 未闭环。
- 只读检查 `ms_image`：20 张 Model 表与物理列/索引/唯一约束一致；主键、comment、无 FK/Enum 规则通过；核心父子孤儿和 current report pointer drift 均为 0。
- 确认四张空 Evaluation 表位于主库，而目标 `ms_image_eval` 不存在；单 Base metadata + 单主库 Alembic 环境未分库。两份 revision 不能从零创建当前 20 表。
- 当前数据盘点：10 张 Image projection 全空；9 个 Task/4 个 Report 中各有 3 行存量非法 `produced`；未写数据库、未回填存量数据。
- 本轮未修改业务源码、业务文档、数据库、表、字段、迁移或测试脚本；只更新 durable handoff 审计状态。
## 2026-08-27 — 四应用全接口逐项审计与 30 号开发清单

- 按真实 FastAPI 路由逐项复核 Runtime 27 个业务 API + 根路由、Admin 6 + 根路由、AI Control 29 + 根路由、Evaluation 9 + 根路由；覆盖请求/响应、鉴权、Service/DalBase 分层、幂等/CAS、状态流与物理表支撑。
- 补充确认 Runtime 问题：ready Study 可新增 Series 且不推进 revision；Session complete/cancel 不联动 Task；Image projection/provenance 四层断链；Task detail 缺 current Report/终态时间；Runtime readiness 错误依赖 Evaluation 平面。
- 补充确认 Control Plane 问题：Prompt import 在 DB read 后等待 Nacos；Connection validate 只做静态结构/hash；AI/Evaluation Control 缺独立 health/readiness；Evaluation create Run 与 Worker owner 冲突、列表无分页。
- 新增 `docs/refactor/30-xray-interface-by-interface-audit-and-development-checklist.md`（1085 行），逐接口标注 KEEP/FIX/BLOCKED/ADD/DEFER，附数据库审计、缺失接口矩阵、开发 Slice、验收卡和源码证据索引。
- 更新 `docs/refactor/README.md` 登记 30 号入口；未修改业务代码、数据库、表、迁移或测试脚本。
